import { FormControl } from '@angular/forms';

export class WordValidator{

    static validWord(fc: FormControl) : Promise<any>{
        const promise = new Promise<any>(
            (resolve, reject) =>{
                return (null);
            }
        );

        return promise;
    }
}